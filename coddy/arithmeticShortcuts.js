// Type your code below
let count = 0
count +=4
count *= 2
count -= 1

// Don't change the line below
console.log(`count = ${count}`)
// count += 4 é o mesmo que count = count + 4
// count *= 2 é o mesmo que count = count * 2
// count -= 1 é o mesmo que count = count - 1